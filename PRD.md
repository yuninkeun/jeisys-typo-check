# 일러스트 파일내 TEXT 오타 검증 서비스 기획서 (PRD)

- 작성자: 윤인근
- 작성일: 2026-09-15
- 최종 업데이트: 2026-09-17 (배포된 실제 구현 기준으로 4~6장 갱신)

## 1. 추진 배경 (문제 정의)

제품전략 부서에서 작성한 요청 내용이 디자인 → 구매 → 생산 단계로 전달되는 과정에서 오탈자가 포함되면, 이 오류가 걸러지지 않은 채 그대로 생산까지 이어져 잘못된 문구가 담긴 스티커가 실제로 부착되는 문제가 발생한다. 특히 제품전략과 디자인 부서에서 새로 만들어진 일러스트 파일은 이후 여러 부서를 거치면서도 별도의 검증 단계가 없어, TEXT 오탈자가 생산 직전까지 발견되지 않을 위험이 크다.

## 2. 현행 방식과 한계

현재는 제품전략·디자인 부서에서 작성한 일러스트 파일(TEXT 포함)을 사람이 눈으로 직접 읽으며 오탈자를 확인하고 있다. 이 방식은 검토자의 집중력과 경험에 의존하기 때문에 오탈자를 놓치기 쉽고, 파일 수가 많아지거나 마감이 촉박할 때 검토가 생략되거나 형식적으로 이루어지는 경우가 있다. 그 결과 오탈자가 있는 상태로 구매·생산 단계까지 넘어가는 일이 발생한다.

## 3. 목표와 기대효과 (성공 기준)

일러스트 파일의 TEXT 오탈자를 생산·판매 이전 단계에서 100% 검출하여, 오탈자가 있는 상태로 생산·판매까지 이어지는 사고를 0건으로 만든다. 또한 사람이 직접 읽어 검토하던 방식을 자동 검증으로 대체하여, 파일 1건당 검증 소요 시간을 1분 이내로 단축한다.

- 오탈자 유출(생산·판매 이후 발견) 건수: 0건/분기
- 파일 1건당 검증 소요 시간: 1분 이내
- 품질검사 단계 이전 사전 검출률: 100%

## 4. 사용자와 이용 흐름

**사용자**: 제품전략·디자인 부서 담당자 (일러스트 파일을 새로 만들거나 수정한 사람). 사내 이메일(도메인 제한)로 가입한 계정으로만 로그인해 사용한다.

**이용 흐름**:
담당자가 사내 계정으로 로그인 → 완성된 일러스트 파일(이미지 PNG/JPG 또는 PDF)을 업로드 → 시스템이 파일 내 TEXT를 추출하고, 맞춤법 오류와 과거 이력 대비 표기 불일치를 검증 → 오탈자 발견 시 원본과 수정 제안을 나란히(As-Is/To-Be) 보여주고, 해당 텍스트가 있는 페이지 번호를 함께 표시 → 담당자가 제안을 참고해 원본 디자인 파일에서 직접 수정 → 검증 이력은 언제든 다시 조회할 수 있으며, 업로드한 사람과 기간(최대 6개월)으로 필터링해 찾아볼 수 있다.

> 파일을 자동으로 고쳐서 돌려주는 것은 아니다 — AI는 "여기가 이상하고, 이렇게 고치면 될 것 같다"는 제안만 하고, 실제 반영은 담당자가 원본 파일에서 직접 한다.

## 5. 주요 기능 (must / nice로 구분)

**[must 1] 일러스트 파일 내 TEXT 오탈자 자동 검증**
- AI가 지킬 규칙:
  - 파일 내 모든 텍스트를 누락 없이 100% 추출하여 검사한다.
  - 검증 결과는 오탈자가 있는 페이지 번호와 원본 텍스트를 함께 표시한다.
    (페이지 내 정확한 좌표 표시는 시도했으나, AI가 추정한 좌표가 반복되는 레이아웃에서
    한 줄씩 어긋나는 경우가 있어 신뢰하기 어렵다고 판단해 페이지 번호 단위로 단순화했다.)
  - 파일 1건당 검증 소요 시간은 1분을 넘지 않는다.
  - 제품명·고유명사 등 확신도가 낮은 항목은 오탈자로 단정하지 않고 "확인 필요"로만 표시한다.
  - 과거에 검증한 같은 제품의 텍스트 이력과 표기가 다르면(예: "Q 2.0"이어야 할 것이
    "QS 2.0"으로 표기됨) 이를 우선적으로 지적한다.

**[must 2] 오탈자 발견 시 수정 제안 표시**
- AI가 지킬 규칙:
  - AI는 원본 파일을 직접 수정하지 않는다 — 텍스트로 된 수정 제안만 보여주고,
    실제 반영 여부와 방법은 전적으로 담당자가 원본 디자인 파일에서 판단·수행한다.
  - 수정 제안은 원본과 나란히(As-Is/To-Be) 비교해서 보여준다.

**[구현 완료 — 최초 PRD 작성 시점엔 nice였으나 실제로 만든 것]**
- 사내 계정 로그인 (이메일/비밀번호, 사내 도메인 제한 가입)
- 검증 이력 조회, 업로드한 사용자별 필터, 기간 검색(최대 6개월)

**[nice — 아직 구현 안 함]**
- 자주 틀리는 단어·제품명 사전 등록 기능
- 오탈자 발생 통계 대시보드 (부서·제품별 월별 추이)
- 여러 파일 일괄 업로드·검증
- 수정된 파일을 자동 생성해 다운로드하는 기능 (현재는 텍스트 제안까지만 제공)

## 6. 범위와 비범위

**이번에 만드는 것 (범위)**
- 사내 계정 로그인 (이메일/비밀번호, `@jeisys.com` 도메인 제한 가입)
- 일러스트 파일 업로드(이미지 PNG/JPG, PDF) 및 TEXT 추출
- TEXT 오탈자 자동 검증 (과거 검증 이력과의 표기 일관성 비교 포함)
- 오탈자 발견 시 수정 제안(As-Is/To-Be) 표시 — 페이지 번호까지만 안내, 실제 파일 수정은
  담당자가 원본에서 직접 수행
- 검증 이력 조회 (업로드한 사용자별 필터, 기간 검색 최대 6개월)

**절대 만들지 않는 것 (비범위)**
- 일러스트의 디자인·레이아웃·색상·폰트·글자 크기 등 텍스트 내용 외 요소 변경
- 수정된 내용을 반영한 파일을 자동으로 생성해 다운로드해주는 기능 (텍스트 제안까지만 제공)
- 구매·생산 시스템과의 자동 연동(파일 자동 전송 등)
- 다국어 번역·현지화 검증 (오탈자 검증에 한정, 번역 품질은 다루지 않음)
- 관리자/일반 사용자 구분 등 역할 기반 세부 권한 관리 (현재는 로그인한 사내 계정이면
  모든 이력을 동일하게 조회 가능)

## 7. 보안·개인정보 검토

검증 대상인 일러스트 파일과 그 안의 TEXT는 사내 제품 정보(제품명, 문구 등)로 외부에 공개되지 않은 자료이며, 개인정보(고객 정보 등)는 포함되지 않는다.

TEXT 추출·오탈자 판단은 OpenAI API로 처리하므로, 업로드한 파일 내용이 외부 사업자(OpenAI)의 API로 전송된다 — "외부에 전송하지 않는다"가 아니라 "고객 개인정보가 없는 사내 제품 표기 정보만, 필요한 처리(추출·판단)를 위해 지정된 외부 AI API로만 전송하고, 그 외 제3자에게는 전달하지 않는다"가 실제 원칙이다. 로그인·파일 저장·이력 DB는 Supabase(사내 계정 도메인 제한)로 처리한다. API 키·DB 접근 키 등은 `.env`/`.env.local`에 보관하고 Git 저장소에는 올리지 않는다.

## 8. 기술 스택

- **Next.js**: 화면(프론트엔드)과 서버 쪽 코드(서버 액션)를 한 프로젝트에서 함께 처리한다.
- **Supabase**: 로그인(이메일/비밀번호, 도메인 제한), 검증 이력 DB(Postgres), 업로드 파일 저장(Storage)을 담당한다. 과거 이력과의 표기 유사도 비교는 Postgres `pg_trgm` 확장을 사용한다.
- **OpenAI (Responses API)**: 이미지·PDF에서 TEXT를 추출하고 오탈자·표기 불일치를 판단한다.
- **Vercel**: 실제 서비스 배포 환경. `https://my-app-xi-ten-51.vercel.app`
- **고정 이유**: STEP 2에서 `my-app`에 이미 설치를 완료했고, 이후 Vercel로 배포할 예정이라 별도 기술 전환 없이 그대로 이어가기 위함이다.
